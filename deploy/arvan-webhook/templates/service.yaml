apiVersion: v1
kind: Service
metadata:
  name: {{ include "arvan-webhook.fullname" . }}
  labels:
    app: {{ include "arvan-webhook.name" . }}
    chart: {{ include "arvan-webhook.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    app: {{ include "arvan-webhook.name" . }}
    release: {{ .Release.Name }}
